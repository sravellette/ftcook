name: feature-tracking-dev
channels:
  - conda-forge
dependencies:
  - python >= 3.13
  - jupyterlab
  - mystmd
  - jupyterlab-myst
  - scipy
  - numpy
  - xarray
  - metpy
  - scikit-image
  - netcdf4
  - matplotlib < 3.11  # Temporary pin because of a colorbar bug
  - pandas
  - cartopy
  - geopandas
  - s3fs
  - zarr
  - pip
  - pip:
    - pelicanfs